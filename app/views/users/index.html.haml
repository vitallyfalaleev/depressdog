.ui.container
  %h1 Listing users

  %table.ui.celled.table
    %thead
      %tr
        %th Name
        %th Email
    %tbody
      - @users.each do |user|
        %tr
          %td= link_to user.name, user
          %td= user.email