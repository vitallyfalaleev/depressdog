.comment
  .content
    = link_to comment.user.name, user_path(comment.user.id), class: 'author'
    .metadata
      %span.date= comment.created_at.to_s(:ru_datetime)
    .text= comment.body
    .actions= link_to 'reply'